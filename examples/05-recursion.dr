# Call a function within itself to create recursion.
# Let's write a function that calculates the n-th Fibonacci number.

function fib(n)
	if n == 0 or n == 1
		ret_val = 1
	else
		ret_val = fib(n - 1) + fib(n - 2)
	endif
	return ret_val
	
println fib(5)

